{
  "type": "script",
  "enabled": true,
  "name": "DeepSeek V4 缓存优化器",
  "id": "72444e03-1a3d-4a92-a75c-aa466db2fbc9",
  "content": "(() => {\n    // ========== CONSTANTS ==========\n    const RUNTIME_KEY = '__deepseekCacheOptimizerRuntime';\n    const FETCH_KEY = '__deepseekCacheOptimizerOriginalFetch';\n    const CONFIG_KEY = 'deepseek_cache_optimizer';\n    const OLD_ATTACH_CONFIG_KEY = 'attach_existing_message_helper';\n    const EDIT_BUTTON_NAME = '缓存优化设置';\n    const TOGGLE_BUTTON_NAME = '缓存优化开关';\n    const ATTACH_MODE_BUTTON_NAME = '附着模式';\n    const LOG_TAG = '[deepseek-cache-optimizer]';\n    const JOINER = '\\n\\n';\n    const WI_CAPTURE_KEY = '__deepseekWILightsCapture';\n\n    const PRESET_MODE = {\n        IMMERSIVE: 'immersive',\n        ANALYSIS: 'analysis',\n        FORMAT_ENFORCE: 'format_enforce',\n        CUSTOM: 'custom',\n    };\n    const PRESET_PROMPTS = {\n        [PRESET_MODE.IMMERSIVE]: [\n            '【角色沉浸要求】在你的思考过程（<think>标签内）中，请遵守以下规则：',\n            '1. 请以角色第一人称进行内心独白，用括号包裹内心活动，例如\"（心想：……）\"或\"(内心OS：……)\"',\n            '2. 用第一人称描写角色的内心感受，例如\"我心想\"\"我觉得\"\"我暗自\"等',\n            '3. 思考内容应沉浸在角色中，通过内心独白分析剧情和规划回复',\n        ].join('\\n'),\n        [PRESET_MODE.ANALYSIS]: [\n            '【思维模式要求】在你的思考过程（<think>标签内）中，请遵守以下规则：',\n            '1. 禁止使用圆括号包裹内心独白，例如\"（心想：……）\"或\"(内心OS：……)\"，所有分析内容直接陈述即可',\n            '2. 禁止以角色第一人称描写内心活动，例如\"我心想\"\"我觉得\"\"我暗自\"等，请用分析性语言替代',\n            '3. 思考内容应聚焦于剧情走向分析和回复内容规划，不要在思考中进行角色扮演式的内心戏表演',\n        ].join('\\n'),\n        [PRESET_MODE.FORMAT_ENFORCE]: [\n            '【格式加强要求】请严格遵守上述既定的输出模板和格式要求：',\n            '1. 必须完整按照输出模板（如有<output-template>或类似模板标签）的结构输出所有内容，不得遗漏任何格式部分',\n            '2. 思维链/思考/创作指导部分（如有要求）必须完整且充分，覆盖所有规定的思考模块',\n            '3. 正文部分必须严格遵守段落格式、字数要求、标签结构等既定规范',\n            '4. 所有后置输出项（如总结、伏笔、大纲、辩证、逻辑加强、选项等，按实际模板要求）必须完整输出，不得省略',\n            '5. 格式标记和XML标签必须正确使用和闭合，整体结构完整无缺',\n        ].join('\\n'),\n    };\n\n    // ========== RUNTIME CLEANUP ==========\n    const previousRuntime = globalThis[RUNTIME_KEY];\n    if (previousRuntime && Array.isArray(previousRuntime.stops)) {\n        for (const stop of previousRuntime.stops) {\n            try { stop(); } catch {}\n        }\n    }\n    const runtime = { stops: [] };\n    globalThis[RUNTIME_KEY] = runtime;\n\n    // ========== UTILITY FUNCTIONS ==========\n    function isObject(v) {\n        return v !== null && typeof v === 'object' && !Array.isArray(v);\n    }\n\n    function isDomElement(v) {\n        return Boolean(v) && typeof v === 'object' && Number(v.nodeType) === 1;\n    }\n\n    function getTagName(v) {\n        return isDomElement(v) && typeof v.tagName === 'string' ? v.tagName.toLowerCase() : '';\n    }\n\n    function pushStop(h) {\n        if (h && typeof h.stop === 'function') runtime.stops.push(h.stop);\n    }\n\n    function notify(type, msg) {\n        const t = globalThis.toastr;\n        if (t && typeof t[type] === 'function') { t[type](msg); return; }\n        (type === 'error' ? console.error : console.log)(LOG_TAG, msg);\n    }\n\n    function escapeHtml(v) {\n        return String(v ?? '').replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/\\\"/g, '&quot;').replace(/'/g, '&#39;');\n    }\n\n    function isAffirmative(r) {\n        return r === true || r === SillyTavern?.POPUP_RESULT?.AFFIRMATIVE;\n    }\n\n    function normalizeRole(role) {\n        const r = String(role ?? '').trim().toLowerCase();\n        if (r === 'model') return 'assistant';\n        return r || 'user';\n    }\n\n    function normalizeAttachRole(role) {\n        const r = String(role ?? '').trim().toLowerCase();\n        if (r === 'assistant' || r === 'system') return r;\n        return 'user';\n    }\n\n    function normalizeSide(side) {\n        const s = String(side ?? '').trim().toLowerCase();\n        if (s === 'start' || s === 'prepend') return 'start';\n        return 'end';\n    }\n\n    function normalizeInteger(value, fallback) {\n        const n = Number(value);\n        return Number.isFinite(n) ? Math.trunc(n) : (fallback ?? 1);\n    }\n\n    function normalizePresetMode(mode, fallback) {\n        const m = String(mode ?? '').trim().toLowerCase();\n        if (m === PRESET_MODE.IMMERSIVE || m === PRESET_MODE.ANALYSIS || m === PRESET_MODE.FORMAT_ENFORCE || m === PRESET_MODE.CUSTOM) return m;\n        return fallback ?? PRESET_MODE.IMMERSIVE;\n    }\n\n    function getPresetModeLabel(mode) {\n        const m = normalizePresetMode(mode, PRESET_MODE.CUSTOM);\n        if (m === PRESET_MODE.IMMERSIVE) return '沉浸式';\n        if (m === PRESET_MODE.ANALYSIS) return '分析模式';\n        if (m === PRESET_MODE.FORMAT_ENFORCE) return '格式加强';\n        return '自定义';\n    }\n\n    function normalizeText(text) {\n        return String(text ?? '').replace(/\\r/g, '');\n    }\n\n    // ========== TEXT / CONTENT HELPERS ==========\n    function extractText(content) {\n        if (typeof content === 'string') return content;\n        if (Array.isArray(content)) {\n            return content\n                .filter(p => isObject(p) && p.type === 'text')\n                .map(p => String(p.text ?? ''))\n                .join(JOINER);\n        }\n        return '';\n    }\n\n    function hasMediaParts(content) {\n        if (!Array.isArray(content)) return false;\n        return content.some(p => isObject(p) && ['image_url', 'video_url', 'audio_url'].includes(p.type));\n    }\n\n    function buildMergedContent(mergedText, originalContent) {\n        if (!Array.isArray(originalContent) || !hasMediaParts(originalContent)) return mergedText;\n        const parts = [{ type: 'text', text: mergedText }];\n        for (const p of originalContent) {\n            if (isObject(p) && p.type !== 'text') parts.push({ ...p });\n        }\n        return parts;\n    }\n\n    function extractTextFromOutgoingContent(content) {\n        if (typeof content === 'string') return normalizeText(content);\n        if (!Array.isArray(content)) return '';\n        return normalizeText(\n            content\n                .filter(p => isObject(p) && p.type === 'text')\n                .map(p => String(p.text ?? ''))\n                .join(JOINER),\n        );\n    }\n\n    function mergeTextSegments(first, second, joiner) {\n        return [String(first ?? ''), String(second ?? '')].filter(s => s.length > 0).join(joiner);\n    }\n\n    function cloneContentPart(part) {\n        if (!isObject(part)) return part;\n        const next = { ...part };\n        if (isObject(part.image_url)) next.image_url = { ...part.image_url };\n        if (isObject(part.video_url)) next.video_url = { ...part.video_url };\n        if (isObject(part.audio_url)) next.audio_url = { ...part.audio_url };\n        return next;\n    }\n\n    function findLastMatchingIndex(items, predicate) {\n        for (let i = items.length - 1; i >= 0; i--) {\n            if (predicate(items[i], i)) return i;\n        }\n        return -1;\n    }\n\n    function attachToContent(currentContent, attachText, side, joiner) {\n        const text = String(attachText ?? '').trim();\n        if (!text) return currentContent;\n        if (currentContent == null) return text;\n        if (typeof currentContent === 'string') {\n            return side === 'start'\n                ? mergeTextSegments(text, currentContent, joiner)\n                : mergeTextSegments(currentContent, text, joiner);\n        }\n        if (!Array.isArray(currentContent)) return currentContent;\n        const next = currentContent.map(cloneContentPart);\n        const idx = side === 'start'\n            ? next.findIndex(p => isObject(p) && p.type === 'text')\n            : findLastMatchingIndex(next, p => isObject(p) && p.type === 'text');\n        if (idx < 0) return currentContent;\n        const cur = String(next[idx].text ?? '');\n        next[idx] = {\n            ...next[idx],\n            text: side === 'start'\n                ? mergeTextSegments(text, cur, joiner)\n                : mergeTextSegments(cur, text, joiner),\n        };\n        return next;\n    }\n\n    // ========== CONFIG: ATTACH RULES ==========\n    function buildDefaultRule(index, overrides) {\n        const o = isObject(overrides) ? overrides : {};\n        return {\n            id: String(o.id ?? `rule-${index}`).trim() || `rule-${index}`,\n            name: String(o.name ?? `附着规则 ${index}`).trim(),\n            enabled: o.enabled !== false,\n            content: typeof o.content === 'string' ? o.content : '',\n            attach_role: normalizeAttachRole(o.attach_role ?? 'user'),\n            attach_index: normalizeInteger(o.attach_index ?? 1, 1),\n            attach_side: normalizeSide(o.attach_side ?? 'end'),\n        };\n    }\n\n    function buildPresetPrimaryRule(mode) {\n        const m = normalizePresetMode(mode);\n        return buildDefaultRule(1, {\n            id: `deepseek-v4-${m}`,\n            name: `DeepSeek V4：首条 user 消息末尾（${getPresetModeLabel(m)}）`,\n            enabled: true,\n            attach_role: 'user',\n            attach_index: 1,\n            attach_side: 'end',\n            content: PRESET_PROMPTS[m] ?? '',\n        });\n    }\n\n    function createDraftRule(index) {\n        return buildDefaultRule(index, {\n            id: `rule-${Date.now()}-${index}`,\n            name: `附着规则 ${index}`,\n            enabled: true,\n            attach_role: 'user',\n            attach_index: -1,\n            attach_side: 'end',\n        });\n    }\n\n    function normalizeRule(rule, index) {\n        if (!isObject(rule)) return null;\n        return {\n            id: String(rule.id ?? `rule-${index + 1}`).trim() || `rule-${index + 1}`,\n            name: String(rule.name ?? '').trim(),\n            enabled: rule.enabled !== false,\n            content: typeof rule.content === 'string' ? rule.content : '',\n            attach_role: normalizeAttachRole(rule.attach_role ?? rule.role ?? 'user'),\n            attach_index: normalizeInteger(rule.attach_index ?? rule.index ?? 1, 1),\n            attach_side: normalizeSide(rule.attach_side ?? rule.side ?? 'end'),\n        };\n    }\n\n    function detectPresetMode(rules) {\n        const first = Array.isArray(rules) ? rules[0] : null;\n        if (!first) return PRESET_MODE.IMMERSIVE;\n        const content = normalizeText(first.content).trim();\n        if (first.attach_role === 'user' && first.attach_index === 1 && first.attach_side === 'end') {\n            if (content === PRESET_PROMPTS[PRESET_MODE.IMMERSIVE]) return PRESET_MODE.IMMERSIVE;\n            if (content === PRESET_PROMPTS[PRESET_MODE.ANALYSIS]) return PRESET_MODE.ANALYSIS;\n            if (content === PRESET_PROMPTS[PRESET_MODE.FORMAT_ENFORCE]) return PRESET_MODE.FORMAT_ENFORCE;\n        }\n        return PRESET_MODE.CUSTOM;\n    }\n\n    // ========== CONFIG: NORMALIZATION ==========\n    function buildDefaultAttachConfig(mode) {\n        const m = normalizePresetMode(mode);\n        return {\n            enabled: true,\n            joiner: JOINER,\n            preset_mode: m,\n            rules: [buildPresetPrimaryRule(m)],\n        };\n    }\n\n    function normalizeAttachConfig(input) {\n        const s = isObject(input) ? input : {};\n        const d = buildDefaultAttachConfig();\n        const rules = Array.isArray(s.rules)\n            ? s.rules.map((r, i) => normalizeRule(r, i)).filter(Boolean)\n            : [];\n        const presetMode = normalizePresetMode(\n            s.preset_mode,\n            detectPresetMode(rules.length > 0 ? rules : d.rules),\n        );\n        return {\n            enabled: s.enabled !== false,\n            joiner: typeof s.joiner === 'string' && s.joiner.length > 0 ? s.joiner : JOINER,\n            preset_mode: presetMode,\n            rules: rules.length > 0 ? rules : d.rules,\n        };\n    }\n\n    function buildDefaultConfig() {\n        return {\n            enabled: true,\n            target_sources: ['deepseek'],\n            role_tags: true,\n            show_cache_stats: true,\n            debug_log: false,\n            separate_wi_lights: false,\n            keep_tail_assistant: true,\n            custom_prefill_enabled: false,\n            custom_prefill_content: '',\n            attach: buildDefaultAttachConfig(),\n        };\n    }\n\n    function normalizeConfig(input) {\n        const s = isObject(input) ? input : {};\n        const d = buildDefaultConfig();\n        return {\n            enabled: s.enabled !== false,\n            target_sources: Array.isArray(s.target_sources) && s.target_sources.length > 0\n                ? s.target_sources.map(x => String(x).trim().toLowerCase()).filter(Boolean)\n                : d.target_sources,\n            role_tags: s.role_tags !== false,\n            show_cache_stats: s.show_cache_stats !== false,\n            debug_log: s.debug_log === true,\n            separate_wi_lights: s.separate_wi_lights === true,\n            keep_tail_assistant: s.keep_tail_assistant !== false,\n            custom_prefill_enabled: s.custom_prefill_enabled === true,\n            custom_prefill_content: typeof s.custom_prefill_content === 'string' ? s.custom_prefill_content : '',\n            attach: normalizeAttachConfig(s.attach),\n        };\n    }\n\n    function cloneConfig(c) {\n        return JSON.parse(JSON.stringify(isObject(c) ? c : buildDefaultConfig()));\n    }\n\n    function applyPresetMode(config, mode, opts) {\n        const next = cloneConfig(config);\n        const m = normalizePresetMode(mode);\n        const resetExtra = isObject(opts) && opts.resetExtraRules === true;\n        const rest = resetExtra ? [] : (Array.isArray(next.attach?.rules) ? next.attach.rules.slice(1) : []);\n        if (!isObject(next.attach)) next.attach = {};\n        next.attach.preset_mode = m;\n        if (m === PRESET_MODE.CUSTOM) return normalizeConfig(next);\n        next.attach.rules = [buildPresetPrimaryRule(m), ...rest];\n        if (typeof next.attach.joiner !== 'string' || !next.attach.joiner) next.attach.joiner = JOINER;\n        return normalizeConfig(next);\n    }\n\n    // ========== CONFIG: STORAGE ==========\n    const STORAGE_OPT = { type: 'extension', extension_id: CONFIG_KEY };\n\n    function getScriptVars() {\n        const v = getVariables(STORAGE_OPT);\n        return isObject(v) ? v : {};\n    }\n\n    function getConfig() {\n        const vars = getScriptVars();\n        const raw = vars[CONFIG_KEY];\n        let config = normalizeConfig(raw);\n        const oldAttach = vars[OLD_ATTACH_CONFIG_KEY];\n        if (isObject(oldAttach) && !(isObject(raw) && isObject(raw.attach))) {\n            config.attach = normalizeAttachConfig(oldAttach);\n        }\n        return config;\n    }\n\n    function saveConfig(config) {\n        updateVariablesWith((vars) => {\n            const next = isObject(vars) ? { ...vars } : {};\n            next[CONFIG_KEY] = normalizeConfig(config);\n            delete next[OLD_ATTACH_CONFIG_KEY];\n            return next;\n        }, STORAGE_OPT);\n        return getConfig();\n    }\n\n    function migrateFromScriptVars() {\n        try {\n            const oldVars = getVariables({ type: 'script' });\n            if (isObject(oldVars) && isObject(oldVars[CONFIG_KEY])) {\n                const currentVars = getScriptVars();\n                if (!isObject(currentVars[CONFIG_KEY])) {\n                    saveConfig(normalizeConfig(oldVars[CONFIG_KEY]));\n                    console.log(LOG_TAG, 'Migrated config from script vars to extension storage');\n                }\n            }\n        } catch (e) { console.log(LOG_TAG, 'Migration check skipped:', e.message); }\n    }\n\n    function ensureConfig() {\n        migrateFromScriptVars();\n        return saveConfig(getConfig());\n    }\n\n    // ========== WI LIGHTS SEPARATION ==========\n    function handleWorldInfoActivated(entries) {\n        if (!Array.isArray(entries)) return;\n        const config = getConfig();\n        if (!config.enabled || !config.separate_wi_lights) return;\n\n        const greenFingerprints = [];\n        for (const entry of entries) {\n            if (!isObject(entry)) continue;\n            if (entry.constant) continue;\n            const content = String(entry.content ?? '').trim();\n            if (!content) continue;\n            greenFingerprints.push(content);\n        }\n\n        globalThis[WI_CAPTURE_KEY] = {\n            greenFingerprints,\n            timestamp: Date.now(),\n        };\n\n        if (config.debug_log && greenFingerprints.length > 0) {\n            console.log(LOG_TAG, 'WI lights captured:', greenFingerprints.length, 'green entries');\n        }\n    }\n\n    function separateWILights(chat, config) {\n        const capture = globalThis[WI_CAPTURE_KEY];\n        globalThis[WI_CAPTURE_KEY] = null;\n\n        if (!capture || !Array.isArray(capture.greenFingerprints) || capture.greenFingerprints.length === 0) return null;\n        if (Date.now() - capture.timestamp > 15000) return null;\n\n        var firstUserIdx = -1;\n        for (var i = 0; i < chat.length; i++) {\n            if (normalizeRole(chat[i].role) === 'user') { firstUserIdx = i; break; }\n        }\n        if (firstUserIdx <= 0) return null;\n\n        var originalOrder = capture.greenFingerprints;\n        var sorted = originalOrder.slice().sort(function(a, b) { return b.length - a.length; });\n        var removedSet = {};\n\n        for (var i = 0; i < firstUserIdx; i++) {\n            var msg = chat[i];\n            if (!isObject(msg)) continue;\n            var text = extractText(msg.content);\n            if (!text) continue;\n\n            var modified = false;\n            for (var fi = 0; fi < sorted.length; fi++) {\n                var fp = sorted[fi];\n                if (removedSet[fp]) continue;\n                if (text.indexOf(fp) >= 0) {\n                    text = text.replace(fp, '');\n                    removedSet[fp] = true;\n                    modified = true;\n                    if (config.debug_log) {\n                        console.log(LOG_TAG, 'WI green removed from preHistory:', fp.substring(0, 60) + '...');\n                    }\n                }\n            }\n\n            if (modified) {\n                text = text.replace(/\\n{3,}/g, '\\n\\n').replace(/^\\n+|\\n+$/g, '').trim();\n                if (typeof msg.content === 'string') {\n                    msg.content = text;\n                } else if (Array.isArray(msg.content)) {\n                    for (var pi = 0; pi < msg.content.length; pi++) {\n                        if (isObject(msg.content[pi]) && msg.content[pi].type === 'text') {\n                            msg.content[pi] = { type: 'text', text: text };\n                            break;\n                        }\n                    }\n                }\n            }\n        }\n\n        for (var i = firstUserIdx - 1; i >= 0; i--) {\n            var text = extractText(chat[i].content).trim();\n            if (!text) chat.splice(i, 1);\n        }\n\n        var removedContents = [];\n        for (var oi = 0; oi < originalOrder.length; oi++) {\n            if (removedSet[originalOrder[oi]]) {\n                removedContents.push(originalOrder[oi]);\n            }\n        }\n\n        if (config.debug_log && removedContents.length > 0) {\n            console.log(LOG_TAG, 'WI separation result:', removedContents.length, 'green entries moved to bottom (original order preserved)');\n        }\n\n        return removedContents.length > 0 ? removedContents : null;\n    }\n\n    // ========== ATTACH ENGINE ==========\n    function buildRawChatHistorySnapshot() {\n        const lastMessageId = Number(getLastMessageId());\n        if (!Number.isInteger(lastMessageId) || lastMessageId < 0) return [];\n        const chatMessages = getChatMessages(`0-${lastMessageId}`, { hide_state: 'unhidden' });\n        const snapshot = [];\n        const ignoreSymbol = SillyTavern?.symbols?.ignore;\n        const settings = isObject(SillyTavern?.chatCompletionSettings) ? SillyTavern.chatCompletionSettings : {};\n        const namesBehavior = Number(settings.names_behavior ?? 0);\n        const userName = String(SillyTavern?.name1 ?? '');\n        const hasGroup = Boolean(SillyTavern?.groupId);\n\n        for (const msg of chatMessages) {\n            if (!isObject(msg)) continue;\n            const extra = isObject(msg.extra) ? msg.extra : {};\n            if (ignoreSymbol && extra[ignoreSymbol]) continue;\n            const role = normalizeAttachRole(msg.role);\n            const name = String(msg.name ?? '');\n            let content = normalizeText(msg.message);\n\n            if (namesBehavior === 0) {\n                const shouldPrefix =\n                    (hasGroup && name.length > 0 && name !== userName) ||\n                    (Boolean(extra.force_avatar) && name.length > 0 && name !== userName && role !== 'system');\n                if (shouldPrefix) content = `${name}: ${content}`;\n            } else if (namesBehavior === 2 && role !== 'system' && name.length > 0) {\n                content = `${name}: ${content}`;\n            }\n\n            const invocations = Array.isArray(extra.tool_invocations) ? extra.tool_invocations.slice() : [];\n            const prev = snapshot[snapshot.length - 1];\n            if (extra.isSmallSys === true && invocations.length > 0 && prev?.role === 'assistant') {\n                prev.invocations = Array.isArray(prev.invocations)\n                    ? prev.invocations.concat(invocations)\n                    : invocations;\n                continue;\n            }\n\n            snapshot.push({\n                message_id: normalizeInteger(msg.message_id, snapshot.length),\n                role, content, name, invocations,\n            });\n        }\n        return snapshot;\n    }\n\n    function matchesRawHistoryMessage(raw, outgoing) {\n        if (!isObject(outgoing)) return false;\n        const outRole = normalizeAttachRole(outgoing.role);\n        if (outRole !== raw.role) return false;\n        if (raw.name && typeof outgoing.name === 'string' && outgoing.name && outgoing.name !== raw.name) return false;\n        const rawText = normalizeText(raw.content);\n        const outText = extractTextFromOutgoingContent(outgoing.content);\n        if (outText === rawText) return true;\n        if (rawText.length > 0 && outText.endsWith(rawText)) return true;\n        return false;\n    }\n\n    function mapRawHistoryToOutgoingChat(chat, rawHistory) {\n        const mapping = new Map();\n        let oi = chat.length - 1;\n        for (let ri = rawHistory.length - 1; ri >= 0 && oi >= 0; ri--) {\n            for (; oi >= 0; oi--) {\n                if (matchesRawHistoryMessage(rawHistory[ri], chat[oi])) {\n                    mapping.set(ri, oi);\n                    oi--;\n                    break;\n                }\n            }\n        }\n        return mapping;\n    }\n\n    function resolveTargetRawMessage(rawHistory, rule) {\n        const matches = [];\n        for (let i = 0; i < rawHistory.length; i++) {\n            if (rawHistory[i].role === rule.attach_role) {\n                matches.push({ rawIndex: i, message: rawHistory[i] });\n            }\n        }\n        if (matches.length === 0) return null;\n        const ni = rule.attach_index === 0 ? 1 : rule.attach_index;\n        const ti = ni > 0 ? ni - 1 : matches.length + ni;\n        if (ti < 0 || ti >= matches.length) return null;\n        return matches[ti];\n    }\n\n    function applyAttachRules(chat, attachConfig) {\n        if (!attachConfig.enabled) return;\n        const rules = attachConfig.rules.filter(\n            r => r.enabled && String(r.content ?? '').trim().length > 0,\n        );\n        if (rules.length === 0) return;\n\n        const rawHistory = buildRawChatHistorySnapshot();\n        if (rawHistory.length === 0) return;\n\n        const mapping = mapRawHistoryToOutgoingChat(chat, rawHistory);\n        for (const rule of rules) {\n            const target = resolveTargetRawMessage(rawHistory, rule);\n            if (!target) continue;\n            const oi = mapping.get(target.rawIndex);\n            if (!Number.isInteger(oi)) continue;\n            const msg = chat[oi];\n            if (!isObject(msg)) continue;\n            const next = attachToContent(msg.content, rule.content, rule.attach_side, attachConfig.joiner);\n            if (next !== msg.content) msg.content = next;\n        }\n    }\n\n    // ========== RESTRUCTURE ENGINE ==========\n    function isToolRelated(msg) {\n        return normalizeRole(msg.role) === 'tool' || Boolean(msg.tool_calls) || Boolean(msg.tool_call_id);\n    }\n\n    function groupEntries(entries) {\n        var groups = [];\n        for (var i = 0; i < entries.length; i++) {\n            var e = entries[i];\n            var last = groups.length > 0 ? groups[groups.length - 1] : null;\n            if (last && last.role === e.role) {\n                last.texts.push(e.text);\n            } else {\n                groups.push({ role: e.role, texts: [e.text] });\n            }\n        }\n        return groups;\n    }\n\n    function buildSections(groups, config) {\n        var sections = [];\n        for (var i = 0; i < groups.length; i++) {\n            var g = groups[i];\n            var content = g.texts.join(JOINER);\n            if (config.role_tags) {\n                sections.push('<role==' + g.role + '>\\n' + content + '\\n</role==' + g.role + '>');\n            } else {\n                sections.push(content);\n            }\n        }\n        return sections;\n    }\n\n    function restructureMessages(chat, config, greenContents) {\n        if (!Array.isArray(chat) || chat.length === 0) return;\n\n        var firstUserIdx = -1;\n        for (var i = 0; i < chat.length; i++) {\n            if (normalizeRole(chat[i].role) === 'user') { firstUserIdx = i; break; }\n        }\n        if (firstUserIdx < 0) {\n            if (config.debug_log) console.log(LOG_TAG, 'No user message found, skipping');\n            return;\n        }\n\n        var preHistory = chat.slice(0, firstUserIdx);\n        var afterFirstUser = chat.slice(firstUserIdx);\n\n        var lastConvIdx = 0;\n        for (var i = afterFirstUser.length - 1; i >= 0; i--) {\n            if (normalizeRole(afterFirstUser[i].role) === 'user' || isToolRelated(afterFirstUser[i])) {\n                lastConvIdx = i;\n                break;\n            }\n        }\n\n        var chatHistory = afterFirstUser.slice(0, lastConvIdx + 1);\n        var postHistoryMessages = afterFirstUser.slice(lastConvIdx + 1);\n\n        if (config.debug_log) {\n            console.log(LOG_TAG, 'Partition:', {\n                preHistory: preHistory.length,\n                chatHistory: chatHistory.length,\n                postHistory: postHistoryMessages.length,\n            });\n        }\n\n        var preEntries = [];\n        for (var i = 0; i < preHistory.length; i++) {\n            var role = normalizeRole(preHistory[i].role);\n            var text = extractText(preHistory[i].content).trim();\n            if (text) preEntries.push({ role: role, text: text });\n        }\n\n        var postHistoryEntries = [];\n        var tailAssistantMessages = [];\n        for (var i = 0; i < postHistoryMessages.length; i++) {\n            var role = normalizeRole(postHistoryMessages[i].role);\n            var text = extractText(postHistoryMessages[i].content).trim();\n            if (!text) continue;\n            if (role === 'assistant' && config.keep_tail_assistant) {\n                tailAssistantMessages.push({ role: 'assistant', content: text });\n            } else {\n                postHistoryEntries.push({ role: role, text: text });\n            }\n        }\n\n        var firstUserContent = chatHistory[0] ? chatHistory[0].content : undefined;\n        var firstUserText = extractText(firstUserContent).trim();\n\n        var isSingleMessage = (chatHistory.length <= 1);\n        var newMessages = [];\n\n        if (isSingleMessage) {\n            var singleGreenEntries = [];\n            if (Array.isArray(greenContents) && greenContents.length > 0) {\n                for (var gi = 0; gi < greenContents.length; gi++) {\n                    singleGreenEntries.push({ role: 'system', text: greenContents[gi] });\n                }\n            }\n            var allEntries = preEntries.concat(singleGreenEntries).concat(postHistoryEntries);\n            var allSections = buildSections(groupEntries(allEntries), config);\n            if (firstUserText) allSections.push(firstUserText);\n            var mergedText = allSections.join(JOINER);\n            var mergedContent = buildMergedContent(mergedText, firstUserContent);\n            newMessages.push({ role: 'user', content: mergedContent });\n\n            for (var ti = 0; ti < tailAssistantMessages.length; ti++) {\n                newMessages.push(tailAssistantMessages[ti]);\n            }\n            if (config.custom_prefill_enabled && config.custom_prefill_content.trim()) {\n                newMessages.push({ role: 'assistant', content: config.custom_prefill_content.trim() });\n            }\n        } else {\n            // TOP ZONE (cached prefix): preHistory + first user input\n            var preSections = buildSections(groupEntries(preEntries), config);\n            if (firstUserText) preSections.push(firstUserText);\n            var mergedText = preSections.join(JOINER);\n            var mergedContent = buildMergedContent(mergedText, firstUserContent);\n            newMessages.push({ role: 'user', content: mergedContent });\n\n            // MIDDLE ZONE: chat history (extract inline system messages)\n            var extractedSystems = [];\n            for (var i = 1; i < chatHistory.length; i++) {\n                var msg = chatHistory[i];\n                var role = normalizeRole(msg.role);\n                if (role === 'system') {\n                    var text = extractText(msg.content).trim();\n                    if (text) extractedSystems.push({ role: 'system', text: text });\n                    continue;\n                }\n                if (isToolRelated(msg)) {\n                    newMessages.push(msg);\n                    continue;\n                }\n                var newMsg = Object.assign({}, msg);\n                if (msg.role === 'model') newMsg.role = 'assistant';\n                newMessages.push(newMsg);\n            }\n\n            // BOTTOM ZONE (high attention, D1-equivalent): prepend to last user message\n            var greenEntries = [];\n            if (Array.isArray(greenContents) && greenContents.length > 0) {\n                for (var gi = 0; gi < greenContents.length; gi++) {\n                    greenEntries.push({ role: 'system', text: greenContents[gi] });\n                }\n            }\n            var postEntries = greenEntries.concat(extractedSystems).concat(postHistoryEntries);\n            if (postEntries.length > 0) {\n                var postSections = buildSections(groupEntries(postEntries), config);\n                var postText = postSections.join(JOINER);\n\n                var lastUserIdx = -1;\n                for (var i = newMessages.length - 1; i >= 0; i--) {\n                    if (normalizeRole(newMessages[i].role) === 'user') { lastUserIdx = i; break; }\n                }\n\n                if (lastUserIdx >= 0) {\n                    var lastUser = newMessages[lastUserIdx];\n                    if (typeof lastUser.content === 'string') {\n                        lastUser.content = postText + JOINER + lastUser.content;\n                    } else if (Array.isArray(lastUser.content)) {\n                        var textParts = lastUser.content.filter(function(p) { return isObject(p) && p.type === 'text'; });\n                        var mediaParts = lastUser.content.filter(function(p) { return isObject(p) && p.type !== 'text'; });\n                        var existingText = textParts.map(function(p) { return String(p.text || ''); }).join(JOINER);\n                        lastUser.content = [{ type: 'text', text: postText + JOINER + existingText }].concat(mediaParts);\n                    }\n                }\n            }\n\n            for (var ti = 0; ti < tailAssistantMessages.length; ti++) {\n                newMessages.push(tailAssistantMessages[ti]);\n            }\n            if (config.custom_prefill_enabled && config.custom_prefill_content.trim()) {\n                newMessages.push({ role: 'assistant', content: config.custom_prefill_content.trim() });\n            }\n        }\n\n        if (config.debug_log) {\n            console.log(LOG_TAG, 'BEFORE (' + chat.length + ' messages):');\n            chat.forEach(function(m, i) { console.log('  [' + i + '] ' + m.role + ': ' + extractText(m.content).substring(0, 80)); });\n            console.log(LOG_TAG, 'AFTER (' + newMessages.length + ' messages):');\n            newMessages.forEach(function(m, i) { console.log('  [' + i + '] ' + m.role + ': ' + extractText(m.content).substring(0, 80)); });\n        }\n\n        chat.splice(0, chat.length, ...newMessages);\n    }\n\n    // ========== PIPELINE ==========\n    function handlePromptReady(eventData) {\n        if (!isObject(eventData) || eventData.dryRun || !Array.isArray(eventData.chat)) return;\n\n        try {\n            const config = getConfig();\n            if (!config.enabled) return;\n\n            const settings = SillyTavern?.chatCompletionSettings;\n            const source = isObject(settings)\n                ? String(settings.chat_completion_source ?? '').trim().toLowerCase()\n                : '';\n\n            if (!source || !config.target_sources.includes(source)) {\n                if (config.debug_log) console.log(LOG_TAG, 'Skipping: source \"' + source + '\" not in targets');\n                return;\n            }\n\n            // Step 1: attach content to original messages (before restructure)\n            applyAttachRules(eventData.chat, config.attach);\n\n            // Step 1.5: separate WI green lights from preHistory to bottom\n            var greenContents = null;\n            if (config.separate_wi_lights) {\n                greenContents = separateWILights(eventData.chat, config);\n            }\n\n            // Step 2: restructure messages for cache optimization\n            restructureMessages(eventData.chat, config, greenContents);\n        } catch (error) {\n            console.error(LOG_TAG, error);\n            notify('error', '缓存优化脚本执行失败: ' + String(error.message ?? error));\n        }\n    }\n\n    // ========== CACHE STATS ==========\n    function showCacheStats(usage) {\n        if (!isObject(usage)) return;\n        var hit = Number(usage.prompt_cache_hit_tokens) || 0;\n        var miss = Number(usage.prompt_cache_miss_tokens) || 0;\n        var total = Number(usage.prompt_tokens) || (hit + miss);\n        var comp = Number(usage.completion_tokens) || 0;\n        var rate = total > 0 ? ((hit / total) * 100).toFixed(1) : '0.0';\n        var msg = '缓存命中: ' + hit + ' tok (' + rate + '%) | 未命中: ' + miss + ' tok | prompt: ' + total + ' | 生成: ' + comp;\n        console.log(LOG_TAG, 'Cache stats:', usage);\n        notify('info', msg);\n    }\n\n    function installFetchInterceptor() {\n        if (globalThis[FETCH_KEY]) {\n            try { window.fetch = globalThis[FETCH_KEY]; } catch {}\n        }\n\n        var originalFetch = window.fetch;\n        globalThis[FETCH_KEY] = originalFetch;\n\n        window.fetch = async function() {\n            var args = arguments;\n            var response = await originalFetch.apply(this, args);\n\n            try {\n                var config = getConfig();\n                if (!config.enabled || !config.show_cache_stats) return response;\n\n                var url = typeof args[0] === 'string' ? args[0] : (args[0] && args[0].url ? args[0].url : '');\n                if (!url || url.indexOf('/api/backends/chat-completions/generate') < 0) return response;\n\n                var contentType = response.headers.get('content-type') || '';\n                var isStream = contentType.indexOf('text/event-stream') >= 0;\n\n                if (isStream && response.body) {\n                    var buffer = '';\n                    var lastUsage = null;\n                    var decoder = new TextDecoder();\n                    var transform = new TransformStream({\n                        transform: function(chunk, controller) {\n                            controller.enqueue(chunk);\n                            buffer += decoder.decode(chunk, { stream: true });\n                            var lines = buffer.split('\\n');\n                            buffer = lines.pop();\n                            for (var li = 0; li < lines.length; li++) {\n                                var line = lines[li];\n                                if (line.indexOf('data: ') === 0 && line.indexOf('[DONE]') < 0) {\n                                    try {\n                                        var parsed = JSON.parse(line.substring(6));\n                                        if (parsed && parsed.usage) lastUsage = parsed.usage;\n                                    } catch (e) {}\n                                }\n                            }\n                        },\n                        flush: function() {\n                            if (lastUsage) showCacheStats(lastUsage);\n                        },\n                    });\n\n                    var newBody = response.body.pipeThrough(transform);\n                    return new Response(newBody, {\n                        status: response.status,\n                        statusText: response.statusText,\n                        headers: response.headers,\n                    });\n                } else if (!isStream) {\n                    var clone = response.clone();\n                    clone.json().then(function(data) {\n                        if (data && data.usage) showCacheStats(data.usage);\n                    }).catch(function() {});\n                }\n            } catch (e) {\n                console.warn(LOG_TAG, 'Fetch interceptor error:', e);\n            }\n\n            return response;\n        };\n\n        return {\n            stop: function() {\n                if (globalThis[FETCH_KEY]) {\n                    try { window.fetch = globalThis[FETCH_KEY]; } catch {}\n                    delete globalThis[FETCH_KEY];\n                }\n            },\n        };\n    }\n\n    // ========== UI ==========\n    function updateInfo(config) {\n        const attachLabel = config.attach.enabled ? getPresetModeLabel(config.attach.preset_mode) : '关闭';\n        const activeRules = config.attach.rules.filter(\n            r => r.enabled && String(r.content ?? '').trim().length > 0,\n        ).length;\n        var prefillLabel = config.keep_tail_assistant ? '保留' : '合并';\n        if (config.custom_prefill_enabled && config.custom_prefill_content.trim()) {\n            prefillLabel += '+自定义';\n        }\n        replaceScriptInfo(\n            '状态: ' + (config.enabled ? '启用' : '停用')\n            + ' | 目标: ' + config.target_sources.join(', ')\n            + ' | 附着: ' + attachLabel + ' (' + activeRules + '条)'\n            + ' | 蓝绿分离: ' + (config.separate_wi_lights ? '开' : '关')\n            + ' | 尾部助手: ' + prefillLabel,\n        );\n    }\n\n    function renderRuleCard(rule, ri) {\n        return '<div style=\"display:grid;gap:10px;padding:12px;border:1px solid var(--SmartThemeBorderColor);border-radius:12px;background:var(--SmartThemeBlurTintColor);\">'\n            + '<div style=\"display:flex;align-items:center;justify-content:space-between;gap:8px;\">'\n            + '<div style=\"font-weight:600;\">规则 ' + (ri + 1) + '</div>'\n            + '<button type=\"button\" class=\"menu_button\" style=\"margin:0;width:auto;white-space:nowrap;\" data-action=\"delete-rule\" data-rule-index=\"' + ri + '\">删除</button>'\n            + '</div>'\n            + '<label style=\"display:flex;align-items:center;gap:8px;\">'\n            + '<input type=\"checkbox\" data-field=\"rule_enabled\" data-rule-index=\"' + ri + '\" ' + (rule.enabled ? 'checked' : '') + '>'\n            + '<span>启用</span></label>'\n            + '<label style=\"display:grid;gap:6px;\">'\n            + '<span>规则名称</span>'\n            + '<input type=\"text\" class=\"text_pole\" style=\"width:100%;\" data-field=\"rule_name\" data-rule-index=\"' + ri + '\" value=\"' + escapeHtml(rule.name) + '\"></label>'\n            + '<div style=\"display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;\">'\n            + '<label style=\"display:grid;gap:6px;\"><span>目标角色</span>'\n            + '<select class=\"text_pole\" style=\"width:100%;\" data-field=\"rule_attach_role\" data-rule-index=\"' + ri + '\">'\n            + '<option value=\"user\"' + (rule.attach_role === 'user' ? ' selected' : '') + '>user</option>'\n            + '<option value=\"assistant\"' + (rule.attach_role === 'assistant' ? ' selected' : '') + '>assistant</option>'\n            + '<option value=\"system\"' + (rule.attach_role === 'system' ? ' selected' : '') + '>system</option>'\n            + '</select></label>'\n            + '<label style=\"display:grid;gap:6px;\"><span>消息序号</span>'\n            + '<input type=\"number\" class=\"text_pole\" style=\"width:100%;\" data-field=\"rule_attach_index\" data-rule-index=\"' + ri + '\" value=\"' + escapeHtml(rule.attach_index) + '\"></label>'\n            + '<label style=\"display:grid;gap:6px;\"><span>拼接位置</span>'\n            + '<select class=\"text_pole\" style=\"width:100%;\" data-field=\"rule_attach_side\" data-rule-index=\"' + ri + '\">'\n            + '<option value=\"start\"' + (rule.attach_side === 'start' ? ' selected' : '') + '>开头</option>'\n            + '<option value=\"end\"' + (rule.attach_side === 'end' ? ' selected' : '') + '>结尾</option>'\n            + '</select></label>'\n            + '</div>'\n            + '<label style=\"display:grid;gap:6px;\"><span>附着内容</span>'\n            + '<textarea class=\"text_pole\" style=\"width:100%;\" data-field=\"rule_content\" data-rule-index=\"' + ri + '\" rows=\"5\" placeholder=\"提示词内容\">' + escapeHtml(rule.content) + '</textarea></label>'\n            + '</div>';\n    }\n\n    function buildEditorMarkup(wc) {\n        const ac = wc.attach;\n        let html = '<div style=\"display:grid;gap:12px;width:min(72vw,960px);max-width:100%;color:var(--SmartThemeBodyColor, #ddd);\">';\n\n        // Section 1: Cache optimizer settings\n        html += '<div style=\"display:grid;gap:8px;padding:12px;border:1px solid var(--SmartThemeBorderColor);border-radius:12px;background:var(--SmartThemeBlurTintColor);\">';\n        html += '<div style=\"font-weight:600;font-size:1.05em;\">缓存优化设置</div>';\n        html += '<div style=\"font-size:0.92em;opacity:0.85;\">'\n            + '设定类消息 → 首条 user（前缀缓存区）<br>'\n            + '对话历史 → 中间（自动缓存）<br>'\n            + '指令类消息（D0-D5） → 末条 user（底部高注意力区，等效 D1）<br>'\n            + '每段用 &lt;role==X&gt; 标签保留原始角色。</div>';\n        html += '<label style=\"display:flex;align-items:center;gap:8px;\">'\n            + '<input type=\"checkbox\" data-field=\"enabled\" ' + (wc.enabled ? 'checked' : '') + '>'\n            + '<span>启用缓存优化（总开关）</span></label>';\n        html += '<label style=\"display:flex;align-items:center;gap:8px;\">'\n            + '<input type=\"checkbox\" data-field=\"role_tags\" ' + (wc.role_tags ? 'checked' : '') + '>'\n            + '<span>添加角色标签 (&lt;role==system&gt; 等)</span></label>';\n        html += '<label style=\"display:flex;align-items:center;gap:8px;\">'\n            + '<input type=\"checkbox\" data-field=\"show_cache_stats\" ' + (wc.show_cache_stats ? 'checked' : '') + '>'\n            + '<span>显示缓存命中统计</span></label>';\n        html += '<label style=\"display:flex;align-items:center;gap:8px;\">'\n            + '<input type=\"checkbox\" data-field=\"debug_log\" ' + (wc.debug_log ? 'checked' : '') + '>'\n            + '<span>调试日志</span></label>';\n        html += '<label style=\"display:flex;align-items:center;gap:8px;\">'\n            + '<input type=\"checkbox\" data-field=\"separate_wi_lights\" ' + (wc.separate_wi_lights ? 'checked' : '') + '>'\n            + '<span>世界书蓝绿灯分离（蓝灯→缓存区，绿灯→高注意力区）</span></label>';\n        html += '<label style=\"display:flex;align-items:center;gap:8px;\">'\n            + '<input type=\"checkbox\" data-field=\"keep_tail_assistant\" ' + (wc.keep_tail_assistant ? 'checked' : '') + '>'\n            + '<span>保留尾部 assistant 消息（预填充/prefill 兼容）</span></label>';\n        html += '<label style=\"display:flex;align-items:center;gap:8px;\">'\n            + '<input type=\"checkbox\" data-field=\"custom_prefill_enabled\" ' + (wc.custom_prefill_enabled ? 'checked' : '') + '>'\n            + '<span>自定义预填充（在末尾追加 assistant 消息）</span></label>';\n        html += '<label style=\"display:grid;gap:6px;\">'\n            + '<span>预填充内容</span>'\n            + '<textarea class=\"text_pole\" style=\"width:100%;\" data-field=\"custom_prefill_content\" rows=\"3\" placeholder=\"自定义预填充内容，留空则不追加\">' + escapeHtml(wc.custom_prefill_content) + '</textarea></label>';\n        html += '<label style=\"display:grid;gap:6px;\">'\n            + '<span>生效的 API 来源（逗号分隔）</span>'\n            + '<input type=\"text\" class=\"text_pole\" style=\"width:100%;\" data-field=\"target_sources\" value=\"' + escapeHtml(wc.target_sources.join(', ')) + '\" placeholder=\"deepseek, custom\">'\n            + '</label>';\n        html += '</div>';\n\n        // Section 2: Attach settings\n        html += '<div style=\"display:grid;gap:8px;padding:12px;border:1px solid var(--SmartThemeBorderColor);border-radius:12px;background:var(--SmartThemeBlurTintColor);\">';\n        html += '<div style=\"font-weight:600;font-size:1.05em;\">内容附着设置</div>';\n        html += '<div style=\"font-size:0.92em;opacity:0.85;\">'\n            + '在缓存重组之前，将指定内容附着到聊天历史中的目标消息。<br>'\n            + '附着到首条 user → 进入缓存区 | 附着到末条 user → 进入高注意力区（D1）。</div>';\n        html += '<label style=\"display:flex;align-items:center;gap:8px;\">'\n            + '<input type=\"checkbox\" data-field=\"attach_enabled\" ' + (ac.enabled ? 'checked' : '') + '>'\n            + '<span>启用内容附着</span></label>';\n        html += '<label style=\"display:grid;gap:6px;\"><span>内置模式</span>'\n            + '<select class=\"text_pole\" style=\"width:100%;\" data-field=\"attach_preset_mode\">'\n            + '<option value=\"immersive\"' + (ac.preset_mode === PRESET_MODE.IMMERSIVE ? ' selected' : '') + '>沉浸式</option>'\n            + '<option value=\"analysis\"' + (ac.preset_mode === PRESET_MODE.ANALYSIS ? ' selected' : '') + '>分析模式</option>'\n            + '<option value=\"format_enforce\"' + (ac.preset_mode === PRESET_MODE.FORMAT_ENFORCE ? ' selected' : '') + '>格式加强</option>'\n            + '<option value=\"custom\"' + (ac.preset_mode === PRESET_MODE.CUSTOM ? ' selected' : '') + '>自定义</option>'\n            + '</select></label>';\n        html += '<label style=\"display:grid;gap:6px;\"><span>拼接分隔符</span>'\n            + '<textarea class=\"text_pole\" style=\"width:100%;\" data-field=\"attach_joiner\" rows=\"1\" placeholder=\"默认 \\\\n\\\\n\">' + escapeHtml(ac.joiner) + '</textarea></label>';\n\n        // Attach rules\n        html += '<div style=\"display:flex;align-items:center;justify-content:space-between;gap:8px;flex-wrap:wrap;margin-top:8px;\">'\n            + '<div style=\"font-weight:600;\">附着规则</div>'\n            + '<div style=\"display:flex;gap:8px;flex-wrap:wrap;\">'\n            + '<button type=\"button\" class=\"menu_button\" style=\"margin:0;width:auto;white-space:nowrap;\" data-action=\"preset-immersive\">载入沉浸式</button>'\n            + '<button type=\"button\" class=\"menu_button\" style=\"margin:0;width:auto;white-space:nowrap;\" data-action=\"preset-analysis\">载入分析</button>'\n            + '<button type=\"button\" class=\"menu_button\" style=\"margin:0;width:auto;white-space:nowrap;\" data-action=\"preset-format-enforce\">载入格式加强</button>'\n            + '<button type=\"button\" class=\"menu_button\" style=\"margin:0;width:auto;white-space:nowrap;\" data-action=\"add-rule\">新增规则</button>'\n            + '<button type=\"button\" class=\"menu_button\" style=\"margin:0;width:auto;white-space:nowrap;\" data-action=\"reset-default\">恢复默认</button>'\n            + '</div></div>';\n\n        html += '<div style=\"display:grid;gap:12px;\">';\n        for (var ri = 0; ri < ac.rules.length; ri++) {\n            html += renderRuleCard(ac.rules[ri], ri);\n        }\n        html += '</div>';\n\n        html += '<div style=\"font-size:0.86em;opacity:0.78;\">'\n            + '序号规则：正数从前往后，负数从后往前，0 按 1 处理。'\n            + '</div>';\n        html += '</div>';\n\n        html += '</div>';\n        return html;\n    }\n\n    function buildConfigEditor(initialConfig) {\n        let wc = normalizeConfig(cloneConfig(initialConfig));\n        let container = null;\n        let listenersBound = false;\n\n        function syncPresetMode() {\n            wc.attach.preset_mode = detectPresetMode(wc.attach.rules);\n        }\n\n        function ensureRules() {\n            if (!Array.isArray(wc.attach.rules) || wc.attach.rules.length === 0) {\n                wc.attach.rules = [createDraftRule(1)];\n            }\n        }\n\n        function getMarkup() {\n            ensureRules();\n            syncPresetMode();\n            return buildEditorMarkup(wc);\n        }\n\n        function render() {\n            if (isDomElement(container)) container.innerHTML = getMarkup();\n        }\n\n        function handleChange(event) {\n            const t = event.target;\n            if (!isDomElement(t)) return;\n            const tag = getTagName(t);\n            if (!['input', 'textarea', 'select'].includes(tag)) return;\n            const f = t.dataset?.field;\n            if (!f) return;\n            const ri = t.dataset?.ruleIndex;\n\n            switch (f) {\n                case 'enabled': wc.enabled = Boolean(t.checked); break;\n                case 'role_tags': wc.role_tags = Boolean(t.checked); break;\n                case 'show_cache_stats': wc.show_cache_stats = Boolean(t.checked); break;\n                case 'debug_log': wc.debug_log = Boolean(t.checked); break;\n                case 'separate_wi_lights': wc.separate_wi_lights = Boolean(t.checked); break;\n                case 'keep_tail_assistant': wc.keep_tail_assistant = Boolean(t.checked); break;\n                case 'custom_prefill_enabled': wc.custom_prefill_enabled = Boolean(t.checked); break;\n                case 'custom_prefill_content': wc.custom_prefill_content = String(t.value ?? ''); break;\n                case 'target_sources':\n                    wc.target_sources = String(t.value ?? '').split(',').map(s => s.trim().toLowerCase()).filter(Boolean);\n                    break;\n                case 'attach_enabled': wc.attach.enabled = Boolean(t.checked); break;\n                case 'attach_preset_mode': {\n                    const mode = normalizePresetMode(t.value, PRESET_MODE.CUSTOM);\n                    if (mode === PRESET_MODE.CUSTOM) {\n                        wc.attach.preset_mode = PRESET_MODE.CUSTOM;\n                    } else {\n                        wc = applyPresetMode(wc, mode, { resetExtraRules: false });\n                        render();\n                    }\n                    break;\n                }\n                case 'attach_joiner': wc.attach.joiner = String(t.value ?? ''); break;\n                default:\n                    if (ri !== undefined && ri !== null) {\n                        const idx = Number(ri);\n                        const rule = wc.attach.rules[idx];\n                        if (!rule) break;\n                        switch (f) {\n                            case 'rule_enabled': rule.enabled = Boolean(t.checked); break;\n                            case 'rule_name': rule.name = String(t.value ?? ''); break;\n                            case 'rule_attach_role': rule.attach_role = normalizeAttachRole(t.value); break;\n                            case 'rule_attach_index': rule.attach_index = normalizeInteger(t.value, 1); break;\n                            case 'rule_attach_side': rule.attach_side = normalizeSide(t.value); break;\n                            case 'rule_content': rule.content = String(t.value ?? ''); break;\n                        }\n                        if (idx === 0) syncPresetMode();\n                    }\n            }\n        }\n\n        function handleAction(event) {\n            const raw = event.target;\n            if (!isDomElement(raw) || typeof raw.closest !== 'function') return;\n            const el = raw.closest('[data-action]');\n            if (!isDomElement(el)) return;\n            event.preventDefault();\n            const action = el.dataset.action;\n\n            if (action === 'preset-immersive') {\n                wc = applyPresetMode(wc, PRESET_MODE.IMMERSIVE, { resetExtraRules: false });\n                render();\n                return;\n            }\n            if (action === 'preset-analysis') {\n                wc = applyPresetMode(wc, PRESET_MODE.ANALYSIS, { resetExtraRules: false });\n                render();\n                return;\n            }\n            if (action === 'preset-format-enforce') {\n                wc = applyPresetMode(wc, PRESET_MODE.FORMAT_ENFORCE, { resetExtraRules: false });\n                render();\n                return;\n            }\n            if (action === 'add-rule') {\n                wc.attach.preset_mode = PRESET_MODE.CUSTOM;\n                wc.attach.rules.push(createDraftRule(wc.attach.rules.length + 1));\n                render();\n                return;\n            }\n            if (action === 'reset-default') {\n                const mode = wc.attach.preset_mode === PRESET_MODE.ANALYSIS\n                    ? PRESET_MODE.ANALYSIS\n                    : PRESET_MODE.IMMERSIVE;\n                wc.attach = buildDefaultAttachConfig(mode);\n                render();\n                return;\n            }\n            if (action === 'delete-rule') {\n                const idx = Number(el.dataset.ruleIndex ?? NaN);\n                if (!Number.isInteger(idx)) return;\n                wc.attach.rules.splice(idx, 1);\n                if (wc.attach.rules.length === 0) {\n                    wc.attach = buildDefaultAttachConfig(\n                        wc.attach.preset_mode === PRESET_MODE.ANALYSIS\n                            ? PRESET_MODE.ANALYSIS\n                            : PRESET_MODE.IMMERSIVE,\n                    );\n                } else {\n                    syncPresetMode();\n                }\n                render();\n            }\n        }\n\n        function mount(nextContainer) {\n            container = isDomElement(nextContainer) ? nextContainer : null;\n            if (!isDomElement(container)) return;\n            if (!listenersBound) {\n                container.addEventListener('input', handleChange);\n                container.addEventListener('change', handleChange);\n                container.addEventListener('click', handleAction);\n                listenersBound = true;\n            }\n            render();\n        }\n\n        return {\n            getHtml: () => getMarkup(),\n            mount,\n            getConfig: () => normalizeConfig(cloneConfig(wc)),\n        };\n    }\n\n    async function openConfigEditor() {\n        const editor = buildConfigEditor(getConfig());\n        const result = await SillyTavern.callGenericPopup(\n            editor.getHtml(),\n            SillyTavern.POPUP_TYPE.TEXT,\n            '',\n            {\n                large: true,\n                wide: true,\n                wider: true,\n                leftAlign: true,\n                allowVerticalScrolling: true,\n                okButton: '保存',\n                cancelButton: '取消',\n                onOpen: async (popup) => { editor.mount(popup.content); },\n            },\n        );\n        if (!isAffirmative(result)) return;\n        const saved = saveConfig(editor.getConfig());\n        updateInfo(saved);\n        notify('success', '设置已保存');\n    }\n\n    function toggleEnabled() {\n        const c = getConfig();\n        c.enabled = !c.enabled;\n        const saved = saveConfig(c);\n        updateInfo(saved);\n        notify('success', saved.enabled ? '缓存优化已启用' : '缓存优化已停用');\n    }\n\n    function cycleAttachMode() {\n        const c = getConfig();\n        const modes = [PRESET_MODE.IMMERSIVE, PRESET_MODE.ANALYSIS, PRESET_MODE.FORMAT_ENFORCE, PRESET_MODE.CUSTOM];\n        if (!c.attach.enabled) {\n            c.attach.enabled = true;\n            const next = applyPresetMode(c, PRESET_MODE.IMMERSIVE);\n            const saved = saveConfig(next);\n            updateInfo(saved);\n            notify('success', '附着已启用 → 沉浸式');\n            return;\n        }\n        const currentIdx = modes.indexOf(c.attach.preset_mode);\n        const nextIdx = (currentIdx + 1) % (modes.length + 1);\n        if (nextIdx >= modes.length) {\n            c.attach.enabled = false;\n            const saved = saveConfig(c);\n            updateInfo(saved);\n            notify('success', '附着已关闭');\n        } else {\n            const next = applyPresetMode(c, modes[nextIdx]);\n            const saved = saveConfig(next);\n            updateInfo(saved);\n            notify('success', '附着模式 → ' + getPresetModeLabel(modes[nextIdx]));\n        }\n    }\n\n    // ========== INIT ==========\n    function init() {\n        const config = ensureConfig();\n        appendInexistentScriptButtons([\n            { name: EDIT_BUTTON_NAME, visible: true },\n            { name: TOGGLE_BUTTON_NAME, visible: true },\n            { name: ATTACH_MODE_BUTTON_NAME, visible: true },\n        ]);\n        updateInfo(config);\n\n        pushStop(eventOn(getButtonEvent(EDIT_BUTTON_NAME), () => { void openConfigEditor(); }));\n        pushStop(eventOn(getButtonEvent(TOGGLE_BUTTON_NAME), toggleEnabled));\n        pushStop(eventOn(getButtonEvent(ATTACH_MODE_BUTTON_NAME), cycleAttachMode));\n        pushStop(eventMakeFirst(tavern_events.CHAT_COMPLETION_PROMPT_READY, handlePromptReady));\n        pushStop(eventOn(tavern_events.WORLD_INFO_ACTIVATED, handleWorldInfoActivated));\n\n        var fetchInterceptor = installFetchInterceptor();\n        if (fetchInterceptor) pushStop(fetchInterceptor);\n\n        const oldAttachRuntime = globalThis['__attachExistingMessageHelperRuntime'];\n        if (oldAttachRuntime) {\n            if (Array.isArray(oldAttachRuntime.stops)) {\n                for (const stop of oldAttachRuntime.stops) {\n                    try { stop(); } catch {}\n                }\n                oldAttachRuntime.stops.length = 0;\n            }\n            notify('warning', '已自动停用旧版「附着到已有消息」脚本。附着功能已集成到本脚本中。');\n        }\n    }\n\n    init();\n})();\n",
  "info": "状态: 停用 | 目标: deepseek | 附着: 格式加强 (1条) | 蓝绿分离: 关 | 尾部助手: 保留",
  "button": {
    "enabled": true,
    "buttons": [
      {
        "name": "缓存优化设置",
        "visible": true
      },
      {
        "name": "缓存优化开关",
        "visible": true
      },
      {
        "name": "附着模式",
        "visible": true
      }
    ]
  },
  "data": {},
  "export_with": {
    "data": true,
    "button": true
  }
}