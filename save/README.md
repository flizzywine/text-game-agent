# save

进度存档目录。

程序会把当前完整进度写入：

```text
save/current-state.json
```

内容包括：

- 当前选中的是哪个故事。
- 每个故事的对话记录。
- 人物状态。
- 已启用的故事资料。
- 全局上下文。
- 用户模块开关。
- 候选选项和调试信息。

这里保存的是游玩进度，不保存导入素材原文件。

网页上的“保存游戏”会直接覆盖 `current-state.json`，不会下载文件；“删除存档”会删除这个文件并清空当前浏览器进度。
